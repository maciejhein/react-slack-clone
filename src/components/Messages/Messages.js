import React from "react";

import MessagesList from "./MessagesList";

const Messages = () => {
  return (
    <section>
      <ul>
        <MessagesList />
      </ul>
    </section>
  );
};

export default Messages;
